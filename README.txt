App made with Node.js, express
Database - SQLite

To run:

- Must have Node.js installed (v16.17.0) 
- Open a command prompt at the jokeapp directory and type "npm i"
- Type "node index"
- Open browser and go to address "http://localhost:3000/"-